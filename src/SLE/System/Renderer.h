#pragma once
/*
#include "PlatformConfig.h"

#include <vector>
#include <string>

class Window_cls;

class Renderer
{
public :
	Renderer();
	~Renderer();

	Window_cls* OpenWindow(uint32_t _XSize, uint32_t _YSize, std::string _Name);

	bool Run();

private:

	Window_cls* m_Window = nullptr;
};*/
